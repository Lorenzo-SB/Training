import math                 #imports the math module

pi = 3.14                   #sets variables
x = 1
y = 2
z = 3

print(round(pi))            #rounds pi
print(math.ceil(pi))        #rounds up pi
print(math.floor(pi))       #rounds down pi
print(abs(pi))              #prints the absolute number (distance from number 0)
print(pow(pi,2))            #factors pi by ²
print(math.sqrt(pi))        #prints the squareroot of pi
print(max(x,y,z))           #finds the max in the variables x, y, z
print(min(x,y,z))           #finds the min in the variables x, y, z